<Window x:Class="BaltikaApp.Wpf.PasswordPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Включение режима редактирования"
        Icon="pack://application:,,,/Assets/AppIcon.ico"
        Height="260" Width="540"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" TextWrapping="Wrap"
                   Text="По умолчанию приложение работает в режиме чтения. Введите пароль для временного включения редактирования."
                   Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="Пароль:" Margin="0,0,0,6"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Height="28" Margin="0,0,0,6"/>
        <CheckBox x:Name="ShowCheck" Grid.Row="3" Content="Показать пароль" VerticalAlignment="Top" Margin="0,0,0,12" Checked="OnShowPassword" Unchecked="OnShowPassword"/>
        <TextBox x:Name="VisiblePasswordBox" Grid.Row="2" Height="28" Margin="0,0,0,6" Visibility="Collapsed" TextChanged="OnVisiblePasswordChanged"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Включить" Style="{StaticResource PrimaryButton}" Width="110" Margin="0,0,8,0" Click="OnOk"/>
            <Button Content="Отмена"   Style="{StaticResource NeutralButton}" Width="110" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
